import { GitCompare } from 'lucide-react';
import PlaceholderPage from '@/components/PlaceholderPage';

export default function CompararPage() {
  return <PlaceholderPage icon={GitCompare} title="Comparar Componentes" />;
}

