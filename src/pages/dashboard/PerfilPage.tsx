import { User } from 'lucide-react';
import PlaceholderPage from '@/components/PlaceholderPage';

export default function PerfilPage() {
  return <PlaceholderPage icon={User} title="Mi Perfil" />;
}

