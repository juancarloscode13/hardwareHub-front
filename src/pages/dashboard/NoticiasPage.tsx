import { Newspaper } from 'lucide-react';
import PlaceholderPage from '@/components/PlaceholderPage';

export default function NoticiasPage() {
  return <PlaceholderPage icon={Newspaper} title="Noticias" />;
}

