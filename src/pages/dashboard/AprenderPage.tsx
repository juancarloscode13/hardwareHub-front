import { GraduationCap } from 'lucide-react';
import PlaceholderPage from '@/components/PlaceholderPage';

export default function AprenderPage() {
  return <PlaceholderPage icon={GraduationCap} title="Aprender Hardware" />;
}

