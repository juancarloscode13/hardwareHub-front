import { MessageSquare } from 'lucide-react';
import PlaceholderPage from '@/components/PlaceholderPage';

export default function ForoPage() {
  return <PlaceholderPage icon={MessageSquare} title="Foro" />;
}

